<div class="gform_wrapper">
  <% form_for @container do |f| %>
    <%= f.error_messages %>
    <div class="gform_body">
      <ul class="gform_fields right_label">
        <li class="gfield">
          <%= f.label :tab_id, "Tab", :class => 'gfield_label' %>
          <div class="ginput_container"><%= f.collection_select :tab_id, @tabs, :id, :title %></div>
        </li>
      </ul>
    </p>
  	<%= f.hidden_field :widget_id %>
  	<div class="gform_footer right_label">
  		<input type="image" tabindex="6" src="/images/woo/btn-submit.png">
  	</div>
  <% end %>
</div>